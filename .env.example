VITE_API_URL=/api
VITE_API_PROXY_TARGET=http://localhost:4002
# Optional uploads origin when API is on a different host (production)
# VITE_UPLOADS_ORIGIN=https://api.roadrescue4u.com
# Optional — required only when Web Push backend + VAPID keys exist
# VITE_VAPID_PUBLIC_KEY=
